spring.application.name=VasilePersonalSite

git.username = Luigi0089

app.cors.allowed-origins=http://localhost:4200,https://www.miosito.com

# application.properties / application.yml
github.token=${GITHUB_TOKEN}

#configuarzione dataSource

#spring.datasource.url=${JDBC_URL:jdbc:mysql://localhost/VasilePersonalDB}
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}

spring.datasource.url=jdbc:postgresql://ep-red-frog-a46n6r44-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_aXcS4jI3AzMD

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


chiave.SimpleAES=${CHIAVE_AES}

universal.password=${UNIVERSAL_PASSWORD}

lezioni.notification.to=${MIA_MAIL}

personal.domain=${PERSONAL_DOMAIN: http://localhost:4200}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.default-encoding=UTF-8